torch
transformers>=4.40.0
# trl>=0.12 introduced Dr. GRPO (old_logps/ref_logps); train_grpo.py handles
# this gracefully via a runtime inspection guard, so any recent trl works.
trl>=0.9.0
peft>=0.10.0
accelerate>=0.27.0
bitsandbytes>=0.43.0
datasets>=2.19.0
requests
tqdm
wandb
matplotlib
numpy
