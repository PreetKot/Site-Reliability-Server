torch
transformers
# trl>=0.12 introduces "Dr. GRPO" which changes grpo_accumulated_loss signature;
# pin to 0.11.x to stay on the standard GRPO API that Unsloth expects.
trl==0.11.4
peft
accelerate
requests
tqdm
wandb
unsloth
# If unsloth wheel resolution fails on your platform, install with:
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
