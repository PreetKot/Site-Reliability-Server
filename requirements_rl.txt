torch
transformers>=4.40.0
# GRPOConfig / GRPOTrainer were added in trl 0.9.0.
# train_grpo.py handles both trl<0.12 (tokenizer=) and trl>=0.12
# (processing_class=, use_dr_grpo) via runtime inspection guards.
trl>=0.9.0
peft>=0.10.0
accelerate>=0.27.0
bitsandbytes>=0.43.0
datasets>=2.19.0
requests
tqdm
wandb
matplotlib
numpy
